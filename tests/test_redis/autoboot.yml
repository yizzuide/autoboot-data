#yaml-language-server: $schema=/usr/local/lib/python3.11/site-packages/autoboot/autoboot.schema.json
# yaml-language-server: $schema=../../autoboot_data/autoboot-data-redis.schema.json
autoboot:
  application:
    name: !env APPLICATION_NAME
    module: &module api

  data:
    redis:
      host: !env REDIS_HOST
      port: 6379

